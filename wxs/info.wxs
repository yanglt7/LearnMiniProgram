// js代码
var message = 'Hello world';
var name = 'yanglt';

var sum = function (num1, num2) {
  return num1 + num2
}

// commonjs的模块化导出方式
module.exports = {
  message: message,
  name: name,
  sum: sum
}